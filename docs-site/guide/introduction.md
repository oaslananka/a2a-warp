# Introduction

A2A Warp is an independent TypeScript runtime and toolkit for the Agent2Agent (A2A) protocol. It is not an official Google, Linux Foundation, or a2aproject package.

A2A Warp provides runtime, client, registry, adapter, bridge, CLI, and testing packages for TypeScript projects that implement supported Agent2Agent concepts.
