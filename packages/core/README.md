# @oaslananka/a2a-warp

Core runtime, client APIs, auth, telemetry, storage, and middleware for A2A Warp.

SQLite storage is optional. Install `better-sqlite3` in the application workspace before constructing `SqliteTaskStorage`.
