# Architecture

Package layers flow from protocol types and schemas into core utilities, runtime, transports, client SDK, registry, adapters, bridges, CLI, and apps. Packages import only public APIs across package boundaries.
