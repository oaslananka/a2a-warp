# Install

Requirements:

- Node.js `>=22.22.1 <25`
- pnpm `>=11 <12`

Install the core runtime:

```bash
pnpm add @oaslananka/a2a-warp
```

PowerShell:

```powershell
pnpm add @oaslananka/a2a-warp
```

Optional packages can be added only when a project needs that surface.
