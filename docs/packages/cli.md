# CLI Package

`@oaslananka/a2a-warp-cli` publishes the `a2a-warp` binary.

The CLI includes validation, discovery, message sending, health checks, monitoring, benchmarking, diagnostics, scaffolding, and conformance reporting. See `docs/cli/conformance.md` for the machine-readable JSON and JUnit conformance report formats.
