{
  "name": "a2a-warp-workspace",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.5.2",
  "description": "Independent TypeScript runtime and toolkit for the Agent2Agent protocol.",
  "keywords": [
    "a2a",
    "agent2agent",
    "ai-agents",
    "multi-agent",
    "llm-orchestration",
    "openai",
    "anthropic",
    "langchain",
    "google-adk",
    "crewai",
    "llamaindex",
    "typescript",
    "mcp",
    "protocol",
    "interoperability",
    "nodejs",
    "monorepo",
    "open-source"
  ],
  "engines": {
    "node": ">=22.22.1 <25",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "setup": "corepack prepare pnpm@11.5.1 --activate && pnpm install --frozen-lockfile",
    "clean": "node scripts/clean.mjs",
    "format": "pnpm run format:write",
    "format:write": "prettier --ignore-path .prettierignore --ignore-unknown --write .",
    "format:check": "prettier --ignore-path .prettierignore --ignore-unknown --check .",
    "lint": "pnpm run lint:code && pnpm run lint:md && pnpm run lint:yaml && pnpm run lint:identity",
    "lint:code": "NODE_OPTIONS=\"--max-old-space-size=8192\" eslint .",
    "lint:fix": "eslint . --fix",
    "lint:md": "markdownlint-cli2",
    "lint:yaml": "node scripts/check-yaml.mjs",
    "lint:identity": "node scripts/check-package-names.mjs && node scripts/check-identity.mjs && node scripts/check-forbidden-refs.mjs && node scripts/check-no-generated-artifacts.mjs && node scripts/check-no-secrets.mjs && node scripts/check-issue-template-packages.mjs && node scripts/check-runtime-versions.mjs",
    "typecheck": "pnpm -r --if-present run typecheck && pnpm exec tsc -p tsconfig.test.json --noEmit && pnpm exec tsc -p apps/demo/tsconfig.json --noEmit && pnpm exec tsc -p apps/registry-ui/tsconfig.json --noEmit",
    "test": "pnpm run test:unit && pnpm run test:integration",
    "test:unit": "pnpm run build && vitest run --project unit",
    "test:integration": "pnpm run build && vitest run --project integration --project transport-contract && pnpm run examples:smoke",
    "test:conformance": "pnpm run build && vitest run --project conformance",
    "test:coverage": "pnpm run build && vitest run --project unit --coverage",
    "test:mutation": "stryker run",
    "test:e2e": "pnpm --dir apps/registry-ui run test:e2e",
    "perf:smoke": "pnpm run build && node scripts/run-k6-performance.mjs --profile smoke",
    "perf:load": "pnpm run build && node scripts/run-k6-performance.mjs --profile load",
    "ui:test:a11y": "pnpm --dir apps/registry-ui run test:a11y",
    "build": "pnpm -r --workspace-concurrency=1 --if-present run build",
    "pack:dry-run": "node scripts/check-npm-pack.mjs",
    "docs:dev": "pnpm --dir docs-site run dev",
    "docs:build": "pnpm --dir docs-site run build",
    "docs:preview": "pnpm --dir docs-site run preview",
    "docs:links": "node scripts/check-public-docs-links.mjs",
    "docs:api": "typedoc",
    "docs:commands:generate": "node scripts/generate-command-docs.mjs",
    "docs:generate": "node scripts/generate-api-docs.mjs && pnpm run docs:commands:generate && pnpm run openapi:generate",
    "docs:check": "node scripts/check-docs-commands.mjs && node scripts/check-docs-package-parity.mjs && node scripts/check-architecture-docs.mjs && node scripts/check-adrs.mjs && node scripts/check-compatibility-docs.mjs && node scripts/check-powershell-doc-parity.mjs && pnpm run openapi:check",
    "dev:smoke": "node scripts/dev-smoke.mjs",
    "examples:smoke": "node scripts/run-examples-smoke.mjs",
    "schemas:generate": "pnpm --filter @oaslananka/a2a-warp run build && node scripts/generate-json-schemas.mjs",
    "schemas:check": "pnpm --filter @oaslananka/a2a-warp run build && node scripts/generate-json-schemas.mjs --check",
    "openapi:generate": "pnpm --filter @oaslananka/a2a-warp-registry run build && node scripts/check-openapi.mjs --write",
    "openapi:check": "pnpm --filter @oaslananka/a2a-warp-registry run build && node scripts/check-openapi.mjs",
    "security": "pnpm audit --audit-level high && gitleaks detect --redact --source . --no-git && node scripts/check-reuse.mjs",
    "verify:structure": "node scripts/check-workspace-graph.mjs && node scripts/check-workspace-declarations.mjs && node scripts/check-public-surface.mjs && node scripts/check-command-surface.mjs && node scripts/check-release-config.mjs",
    "gc": "knip && node scripts/check-duplicates.mjs && node scripts/check-no-generated-artifacts.mjs && node scripts/check-docs-package-parity.mjs && node scripts/check-docs-commands.mjs && node scripts/check-public-surface.mjs && node scripts/check-release-config.mjs",
    "verify": "pnpm run lint && pnpm run build && pnpm run typecheck && pnpm run test:coverage && pnpm run test:integration && pnpm run test:mutation && pnpm run pack:dry-run && pnpm run schemas:check && pnpm run docs:build && pnpm run docs:check && pnpm run security && pnpm run verify:structure && pnpm run gc",
    "ui:install:browsers": "pnpm --dir apps/registry-ui exec playwright install --with-deps chromium",
    "ui:test:e2e": "pnpm --dir apps/registry-ui run test:e2e",
    "release:dry-run": "node scripts/check-release-config.mjs",
    "release:artifacts": "node scripts/prepare-release-artifacts.mjs",
    "release:validate": "node scripts/validate-release-config.mjs",
    "release:parity": "node scripts/check-package-registry-parity.mjs",
    "release:state": "node scripts/release-state.mjs",
    "release:ready": "pnpm --filter @oaslananka/a2a-warp-cli run build && node cli/bin/a2a-warp.js release-check",
    "check:name-collisions": "node scripts/check-name-collisions.mjs",
    "check:bundle-sizes": "node scripts/check-bundle-sizes.mjs",
    "check:pre-push": "pnpm run lint && pnpm run build && pnpm run typecheck && pnpm run test:coverage && pnpm run test:integration && pnpm run pack:dry-run && pnpm run schemas:check && pnpm run docs:build && pnpm run docs:check && pnpm run security && pnpm run verify:structure && pnpm run gc",
    "check:packages": "publint && attw --pack .",
    "commitlint": "commitlint --from HEAD~1 --to HEAD",
    "lint:staged": "lint-staged",
    "review:threads": "node scripts/check-review-threads.mjs",
    "classify:failure": "node scripts/classify-gh-failure.mjs",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "node scripts/run-prettier.mjs"
    ],
    "*.{json,md,yml,yaml}": [
      "node scripts/run-prettier.mjs"
    ]
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "0.18.3",
    "@commitlint/cli": "21.0.2",
    "@commitlint/config-conventional": "21.0.2",
    "@eslint/js": "10.0.1",
    "@stryker-mutator/core": "9.6.1",
    "@stryker-mutator/vitest-runner": "9.6.1",
    "@types/node": "22.19.19",
    "@types/supertest": "7.2.0",
    "@vitest/coverage-v8": "4.1.8",
    "eslint": "10.4.1",
    "fast-check": "4.8.0",
    "globals": "17.6.0",
    "husky": "9.1.7",
    "jscpd": "4.2.4",
    "knip": "6.16.1",
    "lint-staged": "17.0.7",
    "markdownlint-cli2": "0.22.1",
    "prettier": "3.8.3",
    "publint": "0.3.21",
    "supertest": "7.2.2",
    "typedoc": "0.28.19",
    "typedoc-plugin-markdown": "4.12.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.60.0",
    "vitest": "4.1.8"
  }
}
